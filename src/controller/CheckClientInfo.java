/*
package controller;

import view.AccessAccount;
import view.CreateAccount;

import javax.swing.*;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;


/**
 * Checks client info to make sure inputs are correct
 */

//public class CheckClientInfo implements ActionListener
//{
  //  CreateAccount client;

 //   String firstName, lastName, social, email;

//*/
/*
    @Override
    public void actionPerformed(ActionEvent e)
    {
        this.firstName = client.getClientFirstName();
        this.lastName = client.getClientLastName();
        this.social = client.getClientSocial();
        this.email = client.getClientEmail();
        if(!isStringEmpty(client))
       {
           new AccessAccount(firstName, lastName, social, email);
       }
    }

    private boolean isStringEmpty(NewAccount account)
    {
        boolean informationCorrect;

        if(account.getClientFirstName().length() == 0) informationCorrect = false;
        else if(account.getClientLastName().length() == 0) informationCorrect = false;
        else if(account.getClientSocial().trim().length() != 9) informationCorrect = false;
        else informationCorrect = true;

        System.out.println("isStringEmpty: " + informationCorrect);
        return  informationCorrect;
    }
*//*


}
*/
